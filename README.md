# 🎨 IMGRED — консольный инструмент для обработки изображений

**IMGRED** — это простая и удобная консольная утилита на C#, позволяющая применять различные графические фильтры к изображениям формата **BMP**.

---

## ✨ Возможности

Поддерживаемые фильтры:

- `--invert` — инверсия цветов
- `--grayscale` — оттенки серого
- `--sepia` — эффект сепии
- `--bright` — изменение яркости (значение вводится вручную)
- `--contrast` — повышение контраста
- `--blur` — размытие (Box Blur)
- `--channels` — ручная корректировка каналов R/G/B
- `--help` — вывод справки

---

## 📘 Использование

### Синтаксис:

`imgred <option> <input.bmp> [output.bmp]`

Если выходной файл не указан — используется значение по умолчанию: `output.bmp`.

---

## 🧪 Примеры использования

### Инверсия цветов:
`imgred --invert input.bmp result.bmp`
